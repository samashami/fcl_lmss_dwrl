# fcl_lmss_dwrl
Federated Continual Learning for vision-based plastic classification on the private DWRL dataset, comparing a fixed baseline, heuristic controller (V4), and an LLM-guided policy controller under non-IID data streams.
